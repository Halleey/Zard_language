main {
    string nome;
    string saudacao;


    //chama o método input(); que espera receber uma entrada do usuário para declarar seu valor
    input(nome);
    input(saudacao);
    //irá gerar uma concatenação de texto + variavel do usuário;
    print(saudacao + " " + nome + "!");
}