main {
    int x = 0;
    string nome;
    nome = input("digite um nome");
    while (x < 5) {
        print("Valor de x: " + x);
        if(x == 3){
        print("Valor de X passou no if " + x);
        }
        x++;
    }
    print("meu nome é: " + nome);
    print("Fim do loop, x = " + x);
}
