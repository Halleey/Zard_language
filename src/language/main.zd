import "src/language/list/List.zd" as f;

main {

    Struct Pessoa {
    string name;
    int age;
    }

    Struct Pessoa p = {age : 19};

    p.name = input();


    function Pessoa addPeople(Pessoa p){
       return p;
    }

    print(addPeople(p));
}
