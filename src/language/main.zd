main {
    int a = 1;

    while(a < 5) {
        int b = a * 2;   # permitido, loop tem escopo próprio
        println(b);

        while(b < 10) {
            int a = 0;  # proibido, shadowing do pai
            b++;
        }

        a++;
    }
}
